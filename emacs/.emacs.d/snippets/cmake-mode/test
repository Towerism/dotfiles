# -*- mode: snippet -*-
#name : test
#key : test
#contributor : Martin
# --
include(gtest)

set(TEST_SRCS
  $0
  )

set(TEST_LIBS
  gtest
  gtest_main
  )

add_executable(unit-tests ${TEST_SRCS})
target_link_libraries(unit-tests ${TEST_LIBS})
