(require 'org-autolist)
(require 'org-bullets)
(require 'evil-org)

(add-hook 'org-mode-hook
          'org-autolist-mode)

(add-hook 'org-mode-hook
          'org-bullets-mode)

(add-hook 'org-mode-hook
          'evil-org-mode

(add-hook 'org-mode-hook
          'org-indent-mode)

(el-init-provide)
